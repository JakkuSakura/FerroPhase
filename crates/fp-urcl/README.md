# fp-urcl

URCL text emitter for FerroPhase LIR.

CLI usage:
- `fp compile ... --backend binary --emitter urcl`

Scope:
- Emits URCL text directly from LIR.
- Produces `.urcl` output files.
- Does not assemble or execute URCL yet; toolchain integration comes later.
