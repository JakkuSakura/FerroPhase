# QA Evidence

This directory stores local QA evidence for reproducibility.

Structure:
- `template.md`: template for a single QA run.
- `<timestamp>_<label>/evidence.md`: filled evidence for a run.

Generate a new run folder:
`scripts/qa_evidence.sh <label>`
